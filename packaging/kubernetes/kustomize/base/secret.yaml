apiVersion: v1
kind: Secret
metadata:
  name: cacti-spine
type: Opaque
stringData:
  # Replace with a real password or use a SecretGenerator / external-secrets operator.
  # Never commit production credentials here.
  db-password: "changeme"
